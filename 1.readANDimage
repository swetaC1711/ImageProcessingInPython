#1. READ an IMAGE

import cv2 #importing of opencv/computer vision library
img = cv2.imread('abc.jpg') #We are reading the image and storing it inside the img variable
cv2.imshow('OUTPUT',img)#we are displaying the image
cv2.waitKey(0)#3000milisecond --> 3sec, by giving 0 it will stays forever
cv2.destroyAllWindows()#it will close all previously open Image windows
